apiVersion: v1
kind: Service
metadata:
  name: {{ include "thechat-api.fullname" . }}
  labels:
    {{- include "thechat-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "thechat-api.selectorLabels" . | nindent 4 }}
